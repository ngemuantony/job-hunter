
# Construct the database URI
from cyberlink import app


# Route to render the home page with all jobs
# Route to render the home page with all jobs
# Route to render the home page with all jobs

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=8080, debug=True)
